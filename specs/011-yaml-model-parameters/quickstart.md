# Quickstart: Live Model Parameters

Set `inference.parameters.font-size` and `inference.parameters.yuv` in `pipe.yml`, then run `uv run nvidia-pipe --config pipe.yml`. Save a new valid value and observe later frames without restarting. Run `uv run python -m unittest discover -s tests -t .` for unit validation.
