# Quickstart

Run a pipeline with `examples/inference.py`, edit and save a valid callback, and observe the next frame use it. Introduce a syntax error and verify logs report the reload error while frames keep flowing. Validate with `uv run python -m unittest discover -s tests -t .`.
